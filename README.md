# Development of this library has indefinitely paused

I have a real job now and I don't have the free time to continue working on the various issues with landing guidance that are currently present.

This repo contains code and crafts. I will not be providing documentation or support on how to use either other than this: some crafts have specific names, and some scripts have the same names. If you `run m.` on a craft whose name has a corresponding script in the library, that script will run.

<!-- 
## Clone into new KSP instance

Open terminal in the directory where `Kerbal Space Program` is installed and do:

```git
git init
git remote add origin git@github.com:ddejohn/ksp.git
git fetch origin
git checkout -b main --track origin/main
``` -->
